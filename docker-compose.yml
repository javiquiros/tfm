version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:5000"
  mongodb:
    image: "mongo:5.0.6"
    ports:
      - 27017:27017
    volumes:
      - ~/apps/mongo:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=S3cret
